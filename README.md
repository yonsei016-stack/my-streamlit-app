git init # git 초기화
git add . # 새로운 파일 반영
git commit -m "first commit" # 수정된 내용 반영
git branch -M main # main 브랜치 만들기
git remote add origin [내깃헙주소] # 업데이트할 원격 저장소 설정
git push -u origin main # 설정된 저장소로 반영된 내용을 내보내기
